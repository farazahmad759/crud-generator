{
  "name": "crud-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "mig-up": "knex migrate:up && knex migrate:up && knex migrate:up && knex migrate:up && knex migrate:up && knex migrate:up",
    "mig-down": "knex migrate:down && knex migrate:down && knex migrate:down && knex migrate:down && knex migrate:down && knex migrate:down"
  },
  "dependencies": {
    "knex": "^0.21.12",
    "mysql": "^2.18.1",
    "mysql2": "^2.2.5",
    "next": "10.0.1",
    "next-connect": "^0.9.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
